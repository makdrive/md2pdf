// Jest setup file
// テストの前後でのグローバル設定

beforeAll(async () => {
  // テスト開始前の設定
});

afterAll(async () => {
  // テスト終了後のクリーンアップ
});

beforeEach(() => {
  // 各テスト前の設定
  jest.clearAllMocks();
});

afterEach(() => {
  // 各テスト後のクリーンアップ
});